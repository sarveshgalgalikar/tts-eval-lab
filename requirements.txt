# Core
gradio>=4.44.0
soundfile>=0.12.1
numpy>=1.24.0

# TTS engines (pyttsx3 needs espeak-ng — see packages.txt)
pyttsx3>=2.90
kokoro>=0.9.4

# XTTS-v2 is optional and heavy; install manually if needed:
# pip install coqui-tts>=0.25.0

# Scorers — CPU-only torch
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchaudio>=2.1.0
faster-whisper>=1.0.0
